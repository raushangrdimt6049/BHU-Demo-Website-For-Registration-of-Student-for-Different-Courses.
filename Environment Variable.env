# ----------------------------------
# Node.js Environment
# ----------------------------------
# Set to 'production' for deployment on services like Render.
# Use 'development' for local work.
NODE_ENV=development

# The port your application will run on. Render provides this automatically.
# You can set a default for local development.
PORT=3000

# ----------------------------------
# Application & Security
# ----------------------------------
# A long, random string used for signing session cookies if you use express-session
# You can generate one here: https://randomkeygen.com/
SESSION_SECRET=your_super_secret_and_long_random_string

# The public URL of your deployed application.
# Used for creating absolute URLs for things like password reset links or payment callbacks.
# Example: https://your-app-name.onrender.com
APP_URL=http://localhost:3000

# ----------------------------------
# Database URL (If you are using one)
# ----------------------------------
# Example for MongoDB Atlas or a Render PostgreSQL instance
# Replace with your actual database connection string.
DATABASE_URL=mongodb+srv://<user>:<password>@cluster.mongodb.net/your-database-name

# ----------------------------------
# Razorpay API Credentials
# ----------------------------------
# Get these from your Razorpay Dashboard: https://dashboard.razorpay.com/#/app/keys
RAZORPAY_KEY_ID=rzp_test_R9gu7rbC2p8saU
RAZORPAY_KEY_SECRET=jnAvO1J3CbF3ZCACmxR1bNtP

# ----------------------------------
# Email Service (Nodemailer with a provider like Gmail, SendGrid, etc.)
# ----------------------------------
# The service name from Nodemailer's well-known list (e.g., 'Gmail', 'SendGrid', 'SES').
# See: https://nodemailer.com/smtp/well-known/
EMAIL_SERVICE=Gmail

# The email address you will send emails from.
EMAIL_USER=your_email@example.com

# The password or app-specific password for your email account.
# For Gmail, you must use an "App Password".
EMAIL_PASS=your_email_password_or_app_password

# The host for your email provider's SMTP server.
# This is often optional if using a well-known service.
EMAIL_HOST=smtp.gmail.com

# The port for the SMTP server (e.g., 465 for SSL, 587 for TLS).
EMAIL_PORT=465
